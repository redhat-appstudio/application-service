apiVersion: apis.kcp.dev/v1alpha1
kind: APIBinding
metadata:
  name: has-binding
spec:
  reference:
    workspace:
      path: $(KCP_WORKSPACE)
      exportName: has
  acceptedPermissionClaims:
      - group: ""
        resource: "secrets"
      - group: ""
        resource: "configmaps"